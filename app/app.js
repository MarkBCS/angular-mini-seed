'use strict';

// Declare app level module which depends on views, and components
angular.module('AngularMiniSeedApp', [
])

  .controller('appController', function($scope) {
    $scope.hello = 'Hello world!';
  });
